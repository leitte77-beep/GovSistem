x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  postgres:
    image: postgres:16-alpine
    logging: *default-logging
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-diario_clone}
      POSTGRES_USER: ${POSTGRES_USER:-diario_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-diario_password}
    ports:
      - "9632:5432"
    volumes:
      - diario_clone_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-diario_user} -d ${POSTGRES_DB:-diario_clone}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - diario-clone-network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    logging: *default-logging
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "9579:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - diario-clone-network
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    logging: *default-logging
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio_admin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minio_password}
    ports:
      - "9990:9000"
      - "9991:9001"
    volumes:
      - diario_clone_miniodata:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - diario-clone-network
    restart: unless-stopped

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    logging: *default-logging
    environment:
      APP_NAME: "Diario Oficial Clone API"
      DEBUG: ${DEBUG:-true}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-diario_clone}
      POSTGRES_USER: ${POSTGRES_USER:-diario_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-diario_password}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minio_admin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minio_password}
      MINIO_BUCKET: ${MINIO_BUCKET:-diario-clone-publicacoes}
      MINIO_SECURE: "false"
      SIGNER_URL: http://signer:8100
      SECRET_KEY: ${SECRET_KEY:-dev-saas-secret-key-change-in-production}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:-dev-internal-key-saas}
      CORS_ORIGINS: '["http://localhost:9202","http://localhost:9200"]'
      ENVIRONMENT: development
    ports:
      - "9201:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - diario-clone-network
    restart: unless-stopped

  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    logging: *default-logging
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: ${POSTGRES_DB:-diario_clone}
      POSTGRES_USER: ${POSTGRES_USER:-diario_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-diario_password}
      REDIS_HOST: redis
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minio_admin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minio_password}
      MINIO_BUCKET: ${MINIO_BUCKET:-diario-clone-publicacoes}
      SIGNER_URL: http://signer:8100
      API_URL: http://api:8000
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:-dev-internal-key-saas}
      SECRET_KEY: ${SECRET_KEY:-dev-saas-secret-key-change-in-production}
    command: sleep infinity
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - diario-clone-network
    restart: unless-stopped

  signer:
    build:
      context: ./signer
      dockerfile: Dockerfile
    logging: *default-logging
    environment:
      SIGNER_PROVIDER: ${SIGNER_PROVIDER:-a1}
    ports:
      - "9210:8100"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8100/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - diario-clone-network
    restart: unless-stopped

  web-admin:
    build:
      context: ./web-admin
      dockerfile: Dockerfile
    logging: *default-logging
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      API_URL: http://api:8000/api/v1
      NEXT_PUBLIC_API_URL: http://localhost:9201/api/v1
      NEXT_PUBLIC_ADMIN_URL: http://localhost:9202
    ports:
      - "9202:3000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - diario-clone-network
    restart: unless-stopped

  web-public:
    build:
      context: ./web-public
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_ADMIN_URL: ${NEXT_PUBLIC_ADMIN_URL:-http://localhost:9202}
    logging: *default-logging
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      API_URL: http://api:8000/api/v1
      NEXT_PUBLIC_API_URL: http://localhost:9201/api/v1
      NEXT_PUBLIC_ADMIN_URL: http://localhost:9202
    ports:
      - "9200:3000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - diario-clone-network
    restart: unless-stopped

networks:
  diario-clone-network:
    driver: bridge

volumes:
  diario_clone_pgdata:
    driver: local
  diario_clone_miniodata:
    driver: local
